# For complete documentation of this file, please see Geany's main documentation
[styling]

# This is  BaCon syntax and since 
# freebasics syntax was completely replaced for bacons syntax
# packaged for the rasberrypi3 but should also work for any 
# other distro using geany 
# by bigbass updated April 24 2021

# Edit these in the colorscheme .conf file instead
default=default
#comment=comment_line
#commentblock=comment
#docline=comment_line_doc
#docblock=comment_doc
#dockeyword=comment_doc_keyword
 

comment=0x808080
comment_doc=0x404000
comment_line=comment
comment_line_doc=comment_doc
comment_doc_keyword=comment_doc,bold
comment_doc_keyword_error=comment_doc,italic
number=number_1
word=keyword_1
string=string_1
preprocessor=preprocessor
operator=operator
identifier=identifier_1
date=number_2
stringeol=string_eol
word2=keyword_2
word3=keyword_3
word4=keyword_4
constant=identifier_2
asm=type
label=label
error=error
hexnumber=number_1
binnumber=number_1

[keywords]
# all items must be in one line
keywords=alarm alias append array as assoc back bappend appending base bg black blue break broadcast bsave by call case catch certificate changedir chunk class clear close cmode collapse collect color compare compiler const continue copy current cursor cyan data declare decr def default delete delim device directory do done dotimes down downto dq elif else end endclass endenum endfork endfunc endfunction endif endrecord endselect endsub endusec enduseh endwith enum eprint eq esc exit explicit fg fi file fn for format forward framework free from func function ge getbyte getfile getline global gosub goto gotoxy green gt gui if imode import in include incr input intense international inverse is isnot join label ldflags le let library lmode local lookup lt magenta makedir map memrewind memstream memtype monitor multicast ne network next node normal off offset omode on open option options other parse poke pragma print proper proto pull push putbyte putline quoted re read reading readln readwrite receiveserver record recursive red redim relate rename repeat reset resize restore resume return rewind run save scroll sctp seed seek select send setenviron setserial signal size sleep socket sort speed split start startpoint static step stop sub swap system tcp textdomain then tls to trace trap tree type udp until up usec useh utf8 var vartype wend whence while white with writeln writing yellow 


preprocessor=#assert  #define defined #else #elseif #endif #endmacro #error #if #ifdef #ifndef #inclib #include #lang lbacon lfltk lfltk_images #libpath #line   #macro once #pragma pragma #print typeof #undef int double float long char short void signed unsigned static number string floating volatile struct false true pi maxrandom retval regln lineno nl cr el cl version argument source null

# user definable keywords
user1=abs acos address and asc asin atn atn2 bit bload ceil cmdline columns cos count day dec deg endfile equal error even exp fileexists filelen filetime filetype floor fork fp getkey getx gety hour iif instr instrrev int isfalse iskey istrue sum sumf  excl len log loop max maxnum memory memtell min minute mod month mypid not now odd or peek pow rad random reap regex rnd rol ror nrkeys hash round rows search second memcheck sgn sin sizeof sqr tan tally tell timer timevalue ucs ulen val wait week year amount eval istoken bytelen isascii basename chop chr concat curdir dirname err exec extract fill getenviron getpeer b64enc hex host hostname inbetween iif intl lcase left load max mid min month nntl os realpath replace reverse right run b64dec spc str tab typeof ucase weekday  token load outbetween flatten insert bin exchange change obtain unflatten uniq sort rev delim head tail append del cut align utf8 proper rotate last first me explode merge 
user2=backend window fullscreen pixel line square circle arc triangle polygon qbezier cbezier paint text getink flip grid ink pen rotation scale move sync quit cls callback waitkey penup pendown penxy pentype turnright turn turnleft resetangle draw loadfont mouse width height refresh init huglib hugoptions quit hide show grab get set notebook button stock toggle check radio entry password mark combo hseparator vseparator frame edit list msgdialog filedialog spin hslider vslider image register method canvas clipboard progressbar callbackx out picture getcolor attach timeout font disable enable focus unfocus screensize key resetkey display property getproperty setproperty frames delay 


[settings]
# default extension used when saving files
extension=bac

# the following characters are these which a "word" can contains, see documentation
#wordchars=_abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789

# single comments, like # in this file
comment_open='
comment_close=
# multiline comments
comment_open=/'
comment_close='/

# set to false if a comment character/string should start at column 0 of a line, true uses any
# indentation of the line, e.g. setting to true causes the following on pressing CTRL+d
	#command_example();
# setting to false would generate this
#	command_example();
# This setting works only for single line comments
comment_use_indent=true

# context action command (please see Geany's main documentation for details)
context_action_cmd=

[indentation]
#width=4
# 0 is spaces, 1 is tabs, 2 is tab & spaces
#type=1

[build_settings]
# %f will be replaced by the complete filename
# %e will be replaced by the filename without extension
# (use only one of it at one time)
compiler=bacon "%f"
run_cmd="./%e"

