/*
 * config.h for the shared gnulib library itself.
 *
 * gnulib's sources all open with #include <config.h>, so the
 * library needs this file even though it has no package identity
 * of its own to declare. Everything is in config-common.h.
 */

#include <config-common.h>
