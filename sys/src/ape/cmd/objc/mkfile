</$objtype/mkfile

install:V:
	mk -f mkfile.bootstrap install

installall:V:
	mk -f mkfile.bootstrap installall

clean:V:
	mk -f mkfile.bootstrap clean

nuke:V:
	mk -f mkfile.bootstrap nuke

# placeholder until I have made mkfile.rebuild work ...
#	if (test -e /$objtype/ape/bin/objc1) mk -f mkfile.rebuild $target
#	if not mk -f mkfile.bootstrap $target





