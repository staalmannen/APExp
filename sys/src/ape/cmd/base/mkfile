</$objtype/mkfile
MKFILES= libutil libutf bin

install:V:
	for (i in $MKFILES)
		@{ rm -f $O.*; mk -f mkfile.$i $target; rm -f $O.* }

clean:V:
	for (i in $MKFILES)
		@{ rm -f $O.*; mk -f mkfile.$i $target; rm -f $O.* }

nuke:V:
	for (i in $MKFILES)
		@{ mk -f mkfile.$i $target }


