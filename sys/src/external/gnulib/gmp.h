/* Generated by import.sh -- see the GL_GENERATE_MINI_GMP_H rule in
   coreutils' lib/gnulib.mk, which this reproduces.

   coreutils' src/basenc.c and src/factor.c include <gmp.h>
   unconditionally. HAVE_GMP_H and HAVE_LIBGMP are both undefined here,
   so gnulib's libgmp module supplies mini-gmp instead, and this wrapper
   is what points at it. */
#include "mini-gmp.h"
#if GNULIB_LIBGMP_MPQ
# include "mini-mpq.h"
#endif
