tree.tag:
		mkdir -p src
		mkdir -p src/arbits
		mkdir -p src/driver
		mkdir -p src/fallback
		mkdir -p src/host
		mkdir -p src/util
		mkdir -p src/internal
		mkdir -p src/logic
		mkdir -p src/output
		mkdir -p src/skin
		mkdir -p src/stoolie
		mkdir -p src/arbits/output
		mkdir -p src/logic/linkcmd
		touch tree.tag
