/*******************************************************************/
/*  slibtool: a strong libtool implementation, written in C        */
/*  Copyright (C) 2016--2025  SysDeer Technologies, LLC            */
/*  Released under the Standard MIT License; see COPYING.SLIBTOOL. */
/*******************************************************************/

#include <fcntl.h>
#include <stdio.h>
#include <slibtool/slibtool.h>
#include "slibtool_driver_impl.h"
#include "slibtool_dprintf_impl.h"
#include "slibtool_errinfo_impl.h"
#include "slibtool_metafile_impl.h"
#include "slibtool_visibility_impl.h"

static int  slbt_create_default_object_wrapper(
	const struct slbt_driver_ctx *	dctx,
	struct slbt_exec_ctx *		ectx)
{
	int					ret;
	int					fdout;
	const struct slbt_source_version *	verinfo;

	slbt_driver_set_ectx(
		dctx,0,
		ectx->x_ltobjname);

	if ((fdout = openat(
			slbt_driver_fdcwd(dctx),
			ectx->x_ltobjname,
			O_RDWR|O_CREAT|O_TRUNC,
			0644)) < 0)
		return SLBT_SYSTEM_ERROR(dctx);

	verinfo = slbt_api_source_version();

	ret = slbt_dprintf(fdout,
		"# libtool compatible object wrapper\n"
		"# Generated by %s (slibtool %d.%d.%d)\n"
		"# [commit reference: %s]\n\n"

		"pic_object='%s'\n"
		"non_pic_object='%s'\n",

		dctx->x_program,
		verinfo->v_major,verinfo->v_minor,verinfo->v_revision,
		verinfo->v_commit,

		(dctx->x_cctx->x_drvflags & SLBT_DRIVER_SHARED)
			? ectx->x_lobjname
			: "none",
		(dctx->x_cctx->x_drvflags & SLBT_DRIVER_STATIC)
			? ectx->x_aobjname
			: "none");

	close(fdout);

	return (ret < 0) ? SLBT_SYSTEM_ERROR(dctx) : 0;
}

static int  slbt_create_compatible_object_wrapper(
	const struct slbt_driver_ctx *	dctx,
	struct slbt_exec_ctx *		ectx)
{
	int					ret;
	int					fdout;
	const struct slbt_source_version *	verinfo;

	slbt_driver_set_ectx(
		dctx,0,
		ectx->x_ltobjname);

	if ((fdout = openat(
			slbt_driver_fdcwd(dctx),
			ectx->x_ltobjname,
			O_RDWR|O_CREAT|O_TRUNC,
			0644)) < 0)
		return SLBT_SYSTEM_ERROR(dctx);

	verinfo = slbt_api_source_version();

	ret = slbt_dprintf(fdout,
		"# %s - a libtool object file\n"
		"# Generated by %s (slibtool %d.%d.%d)\n"
		"# [commit reference: %s]\n"
		"#\n"
		"# Please DO NOT delete this file!\n"
		"# It is necessary for linking the library.\n\n"

		"# Name of the PIC object.\n"
		"pic_object='%s'\n\n"

		"# Name of the non-PIC object\n"
		"non_pic_object='%s'\n",

		ectx->x_ltobjname,
		dctx->x_program,
		verinfo->v_major,verinfo->v_minor,verinfo->v_revision,
		verinfo->v_commit,

		(dctx->x_cctx->x_drvflags & SLBT_DRIVER_SHARED)
			? ectx->x_lobjname
			: "none",
		(dctx->x_cctx->x_drvflags & SLBT_DRIVER_STATIC)
			? ectx->x_aobjname
			: "none");

	close(fdout);

	return (ret < 0) ? SLBT_SYSTEM_ERROR(dctx) : 0;
}

slbt_hidden int slbt_create_object_wrapper(
	const struct slbt_driver_ctx *	dctx,
	struct slbt_exec_ctx *		ectx)
{
	if (dctx->x_cctx->x_drvflags & SLBT_DRIVER_LEGABITS)
		return slbt_create_compatible_object_wrapper(dctx,ectx);
	else
		return slbt_create_default_object_wrapper(dctx,ectx);
}
